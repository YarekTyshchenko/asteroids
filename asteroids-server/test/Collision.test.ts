import {newShip, recalculateShips, Ship} from "../src/model/ship";

describe('collision test', () => {
  test("A ship hitting stationary B ship on X axis", () => {
/*
    const a = newShip("A", 1)
    a.position = {x: 0, y: 0}
    a.velocity.x = 1
    const b = newShip("B", 1)
    b.position = {x: 21, y: 0}

    const ships = new Map<string, Ship>()
    ships.set("A", a)
    ships.set("B", b)

    recalculateShips(ships)
    expect(a.position.x).toBe(1)

    recalculateShips(ships)
    //expect(a.position.x).toBe(2)
*/
  })
})

