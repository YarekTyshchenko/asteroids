name: asteroids
services:
- name: asteroids-server
  source_dir: asteroids-server
  dockerfile_path: asteroids-server/Dockerfile
  environment_slug: node-js
  github:
    branch: master
    deploy_on_push: true
    repo: yarektyshchenko/asteroids
